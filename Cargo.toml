[workspace]
resolver = "2"
members = [
  "token-lending/program",
  "token-lending/cli",
  "staking/program",
  "staking/cli"
]

[workspace.dependencies]
# Pin blake3 to 1.5.x for compatibility with Solana SDK's bundled Rust 1.75
blake3 = "=1.5.5"

[profile.dev]
split-debuginfo = "unpacked"
